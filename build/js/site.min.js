document.addEventListener( 'DOMContentLoaded', function() {
    let splide = new Splide( '.splide', {
        type  : 'fade',
        rewind: true,
        cover: true,
    } );
    splide.mount();
} );